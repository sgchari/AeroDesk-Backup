rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // This single rule grants open access to all documents and collections for development.
    // It allows all queries, including collection group queries.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
